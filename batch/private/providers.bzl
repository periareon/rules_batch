"""rules_batch provider definitions."""

BatInfo = provider(
    doc = "A provider for batch library rules.",
    fields = {},
)

BatBinaryInfo = provider(
    doc = "A provider for batch binary rules.",
    fields = {},
)

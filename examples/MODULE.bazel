"""Example project using rules_batch."""

module(
    name = "rules_batch_examples",
    version = "0.0.0",
)

bazel_dep(name = "rules_batch", version = "0.0.0")
bazel_dep(name = "platforms", version = "1.0.0")

local_path_override(
    module_name = "rules_batch",
    path = "..",
)
